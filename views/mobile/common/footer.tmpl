<!-- layout 闭合标签 -->
</div>

{{if !isApp}}
       {{if (pageName != "itemDetail" || $pageName != "cart" || $pageName != "orderEntry" )&&(!showedGoldenVipCardBanner&&login&&goldenVipCard)}}
		<div class="welcome-modal" id="J_WelcomeModal">
             	<a href="{{commonEnv.mobileMainServerName}}/market/goldenexp/intro.html">
               	<img src="http://cdn1.showjoy.com/images/03/03631a2a591f43e588ff32d6b40ebf1f.png" height="43"/>
             	</a>
             	<i class="close-btn j_CloseBtn"></i>
           </div>
           <div class="welcome-overlay" id="J_WelcomeOverlay"></div>
       {{/if}}

       <div id="footer"> 

       	<!-- 如果没有这个变量，则判断pageName是否是之前不需要显示电脑版切换的pageName,如果是则不显示，如果不是则显示 -->
       	{{if hasSwitch}}
       	{{else}}
           	<!-- #if($pageName == "personalInfo" || $pageName == "addComment" || $pageName == "tradeMsg" || $pageName == "paymode" ||$pageName == "setAddress" || $pageName == "chooseAddress" || $pageName == "point" || $pageName == "userinfo" || $pageName == "cardsDiscount" || $pageName == "redPacketsDiscount" || $pageName == "register" || $pageName == "login" || $pageName == "tryNavi" || $pageName == "cart" || $pageName == "orderList" || $pageName == "userCardsList" || $pageName == "bindSuccess" || $pageName == "pictext" || $pageName == "orderDetail" || $pageName == "mValueDiscount" || $pageName == "validateMobile" || $pageName == "commentList" || $pageName == "beauty" || $pageName == "beauty-detail"|| $pageName == "worldcup_star"|| $pageName == "world_products" || $pageName == "orderEntry" ||$pageName == "grouponOrderEntry"|| $pageName == "changePassword" || $pageName=="tradeMsg" || $pageName=="findPassword" || $pageName=="searchList" || $pageName=="setAddress" || $pageName=="memberUndertake" ||  $pageName=="memberDetail" || $pageName=="grouponTradeMsg" || $pageName=="getInvitor" || $pageName == "codeExchange" ||$pageName=="successInfo" ||$pageName=="inputAliapyInfo" ||$pageName=="validatePwd"||$pageName=="eightBank")
           		#set($hasSwitch="no")
       		#else
       			#set($hasSwitch="yes")
       		#end -->
       	{{/if}}
       	
       	<!-- 如若没有这个变量，或者变量为true时候显示 -->
       	{{if hasSwitch == "yes"}}
           	<p class="footer-nav">
           		<a href="" class="current">触屏版</a>
           		<span class="split">|</span>
           		<a href="">电脑版</a>
          		</p>
       	{{/if}}
       	<p>浙ICP备12027495号</p>
       </div>
{{/if}}